import { Check, Plus } from 'lucide-react'
import type { Page, PlanSummary, Workflow } from '../types'

export function DashboardPage({ setPage, state, workflows, plans }: { setPage: (page: Page) => void; state: 'loading'|'live'|'offline'; workflows: Workflow[]; plans: PlanSummary[] }) {
  return <><div className="page-header"><div><div className="eyebrow">FILE CURATOR</div><h1>Workspace overview</h1><p>Review indexed metadata and virtual plans before touching any files.</p></div><div className="header-actions"><span className={`badge ${state === 'live' ? 'green' : state === 'loading' ? 'blue' : 'red'}`}>{state === 'live' ? 'Live API' : state === 'loading' ? 'Connecting' : 'API unavailable'}</span><button className="button secondary" onClick={() => setPage('sources')}><Plus size={14}/> Add source</button><button className="button primary" onClick={() => setPage('preview')}>Preview plan</button></div></div><div className="stats-grid"><Stat label="Data mode" value="Metadata" meta="Real files remain unchanged" tone="blue"/><Stat label="Safety boundary" value="Frozen plan" meta="Confirmation required" tone="amber"/><Stat label="Workflows" value={String(workflows.length)} meta="Deterministic pipelines" tone="green"/><Stat label="Plans" value={String(plans.length)} meta="Draft, frozen and executed" tone="purple"/></div><section className="panel pipeline-card"><div className="panel-heading"><div><div className="section-kicker">ACTIVE WORKFLOW</div><h2>{workflows[0]?.name || 'Create your first workflow'}</h2></div>{workflows[0]&&<span className="badge blue">Revision {workflows[0].current_revision}</span>}</div><div className="flow-line">{['Scan','Extract','Normalize','Group','Target','Review'].map((step,i)=><div className="flow-step" key={step}><span className={i < 4 ? 'done' : i === 4 ? 'current' : ''}>{i < 4 ? <Check size={11}/> : i + 1}</span><label>{step}</label></div>)}</div><div className="card-footer"><span>Simulation only until a plan is frozen and confirmed</span><button className="button quiet" onClick={() => setPage('pipeline')}>Configure workflow</button></div></section></>
}

function Stat({label,value,meta,tone}:{label:string;value:string;meta:string;tone:string}) { return <div className={`stat ${tone}`}><div className="stat-label">{label}</div><div className="stat-value">{value}</div><div className="stat-meta">{meta}</div></div> }
